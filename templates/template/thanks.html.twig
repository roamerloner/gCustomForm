{% extends 'base.html.twig' %}

{% block title %}Thank You{% endblock %}

{% block body %}
    <div class="container mx-auto p-6 max-w-3xl bg-gray-900 text-white rounded-lg shadow-md">
        <h1 class="text-3xl font-bold mb-6">Thank You!</h1>
        <p class="text-white">Your response has been recorded.</p>
        
        <div class="mt-4">
            <a href="{{ path('homepage') }}" class="text-blue-600 hover:underline">Return to Main Page</a>
        </div>
    </div>
{% endblock %}